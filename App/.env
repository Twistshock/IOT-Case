# Where the backend lives. Expo only exposes EXPO_PUBLIC_* to the app,
# so the prefix is required. Use your machine's LAN IP while developing -
# "localhost" is the phone itself, not your computer.
# After changing this, restart with: npx expo start -c
EXPO_PUBLIC_SERVER_IP=http://192.168.104.10:8080
